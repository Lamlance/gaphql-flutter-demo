query getAllTodos {
  todos {
    description
    id
  }
}

mutation AddTodo($description: String!) {
  addTodo(description: $description) {
    description
    id
  }
}

mutation AddTodo($description: String!) {
  addTodo(description: $description) {
    description
    id
  }
}
